{
	"name": "tonewise",
	"version": "0.1.0",
	"private": true,
	"description": "ToneWise monorepo with gateway and LLM rewrite pipeline",
	"workspaces": [
		"apps/*",
		"packages/*"
	],
	"scripts": {
		"dev:gateway": "npm --workspace @tonewise/gateway run dev",
		"build:gateway": "npm --workspace @tonewise/gateway run build"
	}
}
