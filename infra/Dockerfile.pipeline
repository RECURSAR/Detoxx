FROM python:3.12-slim
WORKDIR /app

COPY apps/pipeline/requirements.txt ./apps/pipeline/requirements.txt
RUN pip install --no-cache-dir -r apps/pipeline/requirements.txt

COPY apps/pipeline/app ./app

EXPOSE 8000
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
